# Coder profile
Runs `build.py` over tracks with product-plan but no build-plan.
Phase 10: skeleton (writes build-plan + skipped verification).
Phase 11: opencode-cli subprocess execution + real file hashes.
